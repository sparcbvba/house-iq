<!DOCTYPE html>
<html lang="nl">
<head>
    <meta charset="UTF-8">
    <title>Installatie Bewerken</title>
    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css">
</head>

<body>
    <div class="container">
        <h2 class="mt-4">Installatie Bewerken</h2>
        <form action="/installations/<%= installation.id %>" method="POST">
            <!-- Naam -->
            <div class="form-group">
                <label for="name">Naam:</label>
                <input type="text" class="form-control" id="name" name="name" value="<%= installation.name %>" required>
            </div>

            <div class="form-group">
                <label for="url">URL:</label>
                <input type="text" class="form-control" id="url" name="url" value="<%= installation.url %>" required>
            </div>

            <!-- Straat -->
            <div class="form-group">
                <label for="street">Straat:</label>
                <input type="text" class="form-control" id="street" name="street" value="<%= installation.street %>" required>
            </div>

            <!-- Huisnummer -->
            <div class="form-group">
                <label for="number">Huisnummer:</label>
                <input type="text" class="form-control" id="number" name="number" value="<%= installation.number %>" required>
            </div>

            <!-- Postcode -->
            <div class="form-group">
                <label for="postal_code">Postcode:</label>
                <input type="text" class="form-control" id="postal_code" name="postal_code" value="<%= installation.postal_code %>" required>
            </div>

            <!-- Plaats -->
            <div class="form-group">
                <label for="city">Plaats:</label>
                <input type="text" class="form-control" id="city" name="city" value="<%= installation.city %>" required>
            </div>

            <!-- Land -->
            <div class="form-group">
                <label for="country">Land:</label>
                <input type="text" class="form-control" id="country" name="country" value="<%= installation.country %>" required>
            </div>

            <button type="submit" class="btn btn-primary">Opslaan</button>
        </form>
        <a href="/dashboard" class="btn btn-secondary mt-3">Terug naar Dashboard</a>
    </div>

    <!-- Bootstrap JS en afhankelijkheden -->
    <script src="https://code.jquery.com/jquery-3.2.1.slim.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.12.9/umd/popper.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/js/bootstrap.min.js"></script>
</body>
</html>
